# NexaFlow — Python Dependencies
# Install with:  pip install -r requirements.txt

# Core (nexaflow.py)
pynput>=1.7.6
pyperclip>=1.8.2
Pillow>=10.0.0
PySimpleGUI==4.60.5.1
tzdata>=2024.1

# Optional (launcher UI / tray)
pystray>=0.19.4
